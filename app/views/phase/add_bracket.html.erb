<h1>Add bracket to <%= @phase.name %></h1>

<% form_for :bracket do |f| %>
  <p>
    <label for="bracket[name]">Bracket name: </label>
      <%= f.text_field :name %>
  </p>
  
  <%= f.submit %>
<% end %>